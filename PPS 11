/*Generate random series in the range 10 to 20 */
#include<stdio.h>
#include<stdlib.h>
#include<time.h>
int main()
{
// Local Declarations 
int range;
//Statements 
srand(time(NULL));
range = (20-10)+1;
printf("%d", rand() % range + 10);
printf("%d", rand() % range + 10);
printf("%d\n", rand() % range +10);
return 0;
//main
}
